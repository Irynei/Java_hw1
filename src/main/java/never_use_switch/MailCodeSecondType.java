package never_use_switch;

public class MailCodeSecondType implements MailCode {

    public void getMailText(MailInfo mailInfo) {
        // 40 lines of code
        System.out.println("don't call us we call you. was generated and sent to " + mailInfo);
    }
}
